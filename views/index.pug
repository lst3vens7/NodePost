extends layout 
block content 

        <form action="/user" method="post">
        p 
        select(name='dropDown', id='')
            each item in items 
                    option(=item.id) #{value=item.id + ' '}#{value=item.name}
        
        buttons 
            input(type='submit', value='click')

        hr

        //p

        //<form action="/api/user/:id" method="put">
        
        //input(type='text' name='nameInput')
        //input(type='text' name='ageInput')
        //buttons
            input(type='submit', value='clicks')